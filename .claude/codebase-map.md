# Codebase Map
> Auto-generated by /map — 2026-03-15

## Stack
- Language: Rust (edition 2024)
- Framework: clap (CLI), rusqlite (SQLite), ort (ONNX Runtime)
- Package manager: Cargo
- Version: 0.4.0

## Directory Structure
```
nark/
├── src/                     # All source code
│   ├── main.rs              # CLI entrypoint — clap parse + command dispatch
│   ├── config.rs            # config.toml parsing (SearchConfig, Bm25Config, BlendWeights, GraphConfig)
│   ├── cli/                 # Command handlers (thin wrappers calling registry/vault)
│   ├── registry/            # SQLite registry operations (search, write, edges, embeddings, etc.)
│   ├── db/                  # DB connection, migrations, seed data
│   ├── embed/               # ONNX embedding engine (bge-base-en-v1.5) + model download
│   ├── types/               # Domain types (Frontmatter, Note, markdown parsing)
│   └── vault/               # Content-addressed storage (BLAKE3 hashing, CAS file I/O)
├── migrations/              # SQL migration files (rusqlite_migration)
│   ├── 01-create-core-tables/
│   ├── 02-note-edges-v2/
│   ├── 03-importance-access-tracking/
│   └── 04-note-embeddings/
├── Cargo.toml               # Dependencies and project metadata
└── CLAUDE.md                # Developer guide
```

## Entry Points
| File | Purpose |
|------|---------|
| `src/main.rs` | CLI entrypoint — parses args via clap, dispatches to `cli::*` handlers |

## Key Modules

### `src/cli/`
| File | Responsibility |
|------|----------------|
| `mod.rs` | `Cli` struct + `Commands` enum (clap derive) — defines all subcommands |
| `search.rs` | Search handler — builds CosineContext, selects SearchMode, calls registry |
| `about.rs` | Quick research — search + body previews in one call |
| `write.rs` | Ingest notes — walks paths/stdin, calls vault + registry |
| `link.rs` | Create typed edges between notes (updates frontmatter + body wikilinks) |
| `links.rs` | Show a note's edge neighborhood (outgoing + incoming) |
| `embed.rs` | Embedding subcommands — init (download) and build (backfill) |
| `read.rs` | Read full note content from vault |
| `peek.rs` | Show note metadata from registry (no vault read) |
| `ls.rs` | Browse knowledge tree: domain → intent → kind → notes |
| `tag.rs` | Add/remove/list/find tags |
| `delete.rs` | Soft/hard/purge delete notes |
| `stats.rs` | Vault statistics overview |
| `init.rs` | Initialize vault directories + registry database |
| `reset.rs` | Reset registry database (drop + recreate) |
| `update.rs` | Pull latest code and rebuild binary |

### `src/registry/`
| File | Responsibility |
|------|----------------|
| `mod.rs` | Module root — re-exports submodules |
| `search.rs` | Search pipeline (BM25 → graph → cosine → blend) + 16 integration tests |
| `write.rs` | `commit_version()` — upsert note, version, current_note, FTS, tags, edges |
| `edges.rs` | Edge CRUD (note_edges table) — `get_edges()`, `sync_edges()` |
| `embeddings.rs` | Embedding storage/retrieval (note_embeddings table) — upsert, get, get_all |
| `access.rs` | Access tracking — `bump_access()` updates count + last_accessed (ACT-R activation) |
| `browse.rs` | `ls`/tree browsing — returns GroupRow or NoteRow at each tree level |
| `stats.rs` | Vault statistics — facet counts, recent notes, most accessed |
| `tags.rs` | Tag CRUD — get/add/remove/list/find operations |
| `resolve.rs` | Head resolution — `get_meta()`, `get_ref()` for version lookups |
| `delete.rs` | Delete logic — validate IDs, soft delete, hard delete, purge CAS objects |

### `src/db/`
| File | Responsibility |
|------|----------------|
| `mod.rs` | `open_registry()` — opens SQLite, applies migrations, seeds defaults |
| `migrate.rs` | (empty) Migration runner placeholder — actual migrations via `rusqlite_migration` |

### `src/embed/`
| File | Responsibility |
|------|----------------|
| `mod.rs` | `EmbeddingEngine` — ONNX session + tokenizer, `embed_document()`, `embed_query()`, `cosine_similarity()` |
| `download.rs` | Download ONNX Runtime + bge-base-en-v1.5 model files with progress + SHA256 verification |

### `src/types/`
| File | Responsibility |
|------|----------------|
| `mod.rs` | Re-exports `markdown` and `note` submodules |
| `markdown.rs` | `Frontmatter` struct + enums (Domain, Intent, Kind, Trust, Status), `FrontmatterLink`, wikilink extraction |
| `note.rs` | `IngestResult` struct — output of vault ingestion (IDs, hashes, parsed content) |

### `src/vault/`
| File | Responsibility |
|------|----------------|
| `mod.rs` | `Vault` struct — `init_dirs()`, `ingest()` (parse + hash + store) |
| `fs.rs` | CAS file I/O — BLAKE3 content addressing, read/write objects |

## Dependency Graph (simplified)
```
main.rs → cli/* → registry/* → db/mod.rs (SQLite)
                → vault/*    → types/* (Frontmatter, Note)
                → embed/*    → ort (ONNX Runtime)
                → config.rs

cli/search.rs → registry/search.rs → registry/embeddings.rs
                                    → registry/edges.rs
                                    → registry/access.rs
                                    → embed/mod.rs (cosine_similarity)

cli/write.rs → vault/mod.rs → vault/fs.rs (BLAKE3 CAS)
             → registry/write.rs → registry/edges.rs
                                 → registry/tags.rs

cli/link.rs → vault/mod.rs (re-ingest with updated frontmatter)
            → registry/write.rs
```
